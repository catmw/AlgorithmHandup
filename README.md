Author: Catherine Whiteside

If there is an issue with running GraphPanel.java try: java -cp bin com.newviewer.GraphPanel

# To Run Tester
Navigate to tester (RedBlackTreeTester.java). If on windows either use F5 or the built in run button to VS Code.

# To Run Graph
Navigate to GraphPanel.java. If on windows either use F5 or the built in run button to VS Code. If using eclipse use the built in run button.
If there is an issue with running GraphPanel.java try java -cp bin com.newviewer.GraphPanel
